"use strict";

var _getStep = _interopRequireDefault(require("./getStep"));

var _roundMinute = _interopRequireDefault(require("./roundMinute"));

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

describe('steps/roundMinute', function () {
  it('should get step correctly', function () {
    var getStepFor = function getStepFor(secondsPassed) {
      return (0, _getStep.default)(_roundMinute.default, secondsPassed, {
        units: ['now', 'second', 'minute', 'hour', 'day', 'week', 'month', 'year']
      });
    }; // expect(getStepFor(0)).to.be.undefined


    expect(getStepFor(0).formatAs).to.equal('now');
    expect(getStepFor(0.5).formatAs).to.equal('now');
    expect(getStepFor(29.9).formatAs).to.equal('now');
    expect(getStepFor(30).formatAs).to.equal('minute');
    expect(getStepFor(59.5 * 60 - 1).formatAs).to.equal('minute');
    expect(getStepFor(59.5 * 60).formatAs).to.equal('hour');
    expect(getStepFor(24 * 60 * 60).formatAs).to.equal('day');
    expect(getStepFor(7 * 24 * 60 * 60).formatAs).to.equal('week');
  });
  it('should use "minute"s when "now" is not allowed', function () {
    var getStepFor = function getStepFor(secondsPassed) {
      return (0, _getStep.default)(_roundMinute.default, secondsPassed, {
        units: ['second', 'minute', 'hour', 'day', 'week', 'month', 'year']
      });
    };

    expect(getStepFor(0).formatAs).to.equal('minute');
  });
});
//# sourceMappingURL=roundMinute.test.js.map