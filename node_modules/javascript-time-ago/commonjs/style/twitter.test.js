"use strict";

var _twitter = _interopRequireDefault(require("./twitter"));

var _TimeAgo = _interopRequireDefault(require("../TimeAgo"));

var _steps = require("../steps");

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

function _objectSpread(target) { for (var i = 1; i < arguments.length; i++) { var source = arguments[i] != null ? arguments[i] : {}; var ownKeys = Object.keys(source); if (typeof Object.getOwnPropertySymbols === 'function') { ownKeys = ownKeys.concat(Object.getOwnPropertySymbols(source).filter(function (sym) { return Object.getOwnPropertyDescriptor(source, sym).enumerable; })); } ownKeys.forEach(function (key) { _defineProperty(target, key, source[key]); }); } return target; }

function _defineProperty(obj, key, value) { if (key in obj) { Object.defineProperty(obj, key, { value: value, enumerable: true, configurable: true, writable: true }); } else { obj[key] = value; } return obj; }

describe('style/twitter', function () {
  it('should fallback from "mini-time" to "narrow"', function () {
    var timeAgo = new _TimeAgo.default('it');
    timeAgo.format(Date.now() - 3 * _steps.hour * 1000, 'twitter').should.equal('3 h fa');
  });
  it('should format Twitter style relative time (English)', function () {
    var timeAgo = new _TimeAgo.default('en'); // April 10th, 2016.

    var now = new Date(2016, 3, 10, 22, 59).getTime();

    var formatInterval = function formatInterval(secondsPassed) {
      return timeAgo.format(now - secondsPassed * 1000, _objectSpread({
        now: now
      }, _twitter.default));
    };

    formatInterval(0).should.equal('0s');
    formatInterval(0.5).should.equal('1s');
    formatInterval(59.4).should.equal('59s');
    formatInterval(59.6).should.equal('1m');
    formatInterval(1.49 * _steps.minute).should.equal('1m');
    formatInterval(1.51 * _steps.minute).should.equal('2m');
    formatInterval(2.49 * _steps.minute).should.equal('2m');
    formatInterval(2.51 * _steps.minute).should.equal('3m'); // …

    formatInterval(59.49 * _steps.minute).should.equal('59m');
    formatInterval(59.51 * _steps.minute).should.equal('1h');
    formatInterval(1.49 * _steps.hour).should.equal('1h');
    formatInterval(1.51 * _steps.hour).should.equal('2h');
    formatInterval(2.49 * _steps.hour).should.equal('2h');
    formatInterval(2.51 * _steps.hour).should.equal('3h'); // …

    formatInterval(23.49 * _steps.hour).should.equal('23h');
    formatInterval(_steps.day + 2 * _steps.minute + _steps.hour).should.equal('Apr 9'); // …
    // `month` is about 30.5 days.

    formatInterval(_steps.month * 3).should.equal('Jan 10');
    formatInterval(_steps.month * 4).should.equal('Dec 11, 2015');
    formatInterval(_steps.year).should.equal('Apr 11, 2015'); // Test future dates.
    // `month` is about 30.5 days.

    formatInterval(-1 * _steps.month * 8).should.equal('Dec 10');
    formatInterval(-1 * _steps.month * 9).should.equal('Jan 9, 2017');
  });
  it('should format Twitter style relative time (Russian)', function () {
    var timeAgo = new _TimeAgo.default('ru');
    var now = new Date(2016, 3, 10, 22, 59).getTime();

    var formatInterval = function formatInterval(secondsPassed) {
      return timeAgo.format(now - secondsPassed * 1000, _objectSpread({
        now: now
      }, _twitter.default));
    };

    formatInterval(0).should.equal('0 с');
    formatInterval(0.5).should.equal('1 с');
    formatInterval(59.51).should.equal('1 мин');
    formatInterval(59.51 * _steps.minute).should.equal('1 ч');
    formatInterval(_steps.day + 62 * _steps.minute).should.equal('9 апр.');
    formatInterval(_steps.year).should.equal('11 апр. 2015 г.');
  });
  it('should format Twitter style relative time (Korean)', function () {
    var timeAgo = new _TimeAgo.default('ko');
    var now = new Date(2016, 3, 10, 22, 59).getTime();

    var formatInterval = function formatInterval(secondsPassed) {
      return timeAgo.format(now - secondsPassed * 1000, _objectSpread({
        now: now
      }, _twitter.default));
    };

    formatInterval(59.51).should.equal('1분');
    formatInterval(59.51 * _steps.minute).should.equal('1시간');
    formatInterval(_steps.day + 62 * _steps.minute).should.equal('4월 9일');
    formatInterval(_steps.year).should.equal('2015년 4월 11일');
  });
  it('should format Twitter style relative time (German)', function () {
    var timeAgo = new _TimeAgo.default('de');
    var now = new Date(2016, 3, 10, 22, 59).getTime();

    var formatInterval = function formatInterval(secondsPassed) {
      return timeAgo.format(now - secondsPassed * 1000, _objectSpread({
        now: now
      }, _twitter.default));
    };

    formatInterval(59.51).should.equal('1 Min.');
    formatInterval(59.51 * _steps.minute).should.equal('1 Std.');
    formatInterval(_steps.day + 62 * _steps.minute).should.equal('9. Apr.');
    formatInterval(_steps.year).should.equal('11. Apr. 2015');
  });
  it('should format Twitter style relative time (French)', function () {
    var timeAgo = new _TimeAgo.default('fr');
    var now = new Date(2016, 3, 10, 22, 59).getTime();

    var formatInterval = function formatInterval(secondsPassed) {
      return timeAgo.format(now - secondsPassed * 1000, _objectSpread({
        now: now
      }, _twitter.default));
    };

    formatInterval(59.51).should.equal('1 min');
    formatInterval(59.51 * _steps.minute).should.equal('1 h');
    formatInterval(_steps.day + 62 * _steps.minute).should.equal('9 avr.');
    formatInterval(_steps.year).should.equal('11 avr. 2015');
  });
  it('should format Twitter style relative time (Chinese)', function () {
    var timeAgo = new _TimeAgo.default('zh');
    var now = new Date(2016, 3, 10, 22, 59).getTime();

    var formatInterval = function formatInterval(secondsPassed) {
      return timeAgo.format(now - secondsPassed * 1000, _objectSpread({
        now: now
      }, _twitter.default));
    };

    formatInterval(59.51).should.equal('1分钟');
    formatInterval(59.51 * _steps.minute).should.equal('1小时');
    formatInterval(_steps.day + 62 * _steps.minute).should.equal('4月9日');
    formatInterval(_steps.year).should.equal('2015年4月11日');
  }); // This test won't pass because `Intl.DateTimeFormat` is read at
  // initialization time, not at run time.
  // it('should fall back to generic style when Intl.DateTimeFormat is not available', () => {
  // 	const DateTimeFormat = Intl.DateTimeFormat
  // 	Intl.DateTimeFormat = undefined
  //
  // 	const timeAgo = new TimeAgo('en')
  // 	timeAgo.format(Date.now() - 365 * 24 * hour * 1000, 'twitter').should.equal('1yr')
  //
  // 	Intl.DateTimeFormat = DateTimeFormat
  // })

  it('should support timestamp argument on `yearMonthAndDay.test()`', function () {
    var timeAgo = new _TimeAgo.default('en');
    timeAgo.format(0, 'twitter').should.equal('Jan 1, 1970');
  });
  it('should return time to next update', function () {
    var timeAgo = new _TimeAgo.default('en');
    var now = new Date(2016, 3, 10, 22, 59).getTime();
    timeAgo.format(now + _steps.day * 1000, 'twitter', {
      now: now,
      getTimeToNextUpdate: true
    }).should.deep.equal(['Apr 11', 22899660000 // The next update is in about 265 days.
    ]);
    timeAgo.format(0, 'twitter', {
      getTimeToNextUpdate: true
    }).should.deep.equal(['Jan 1, 1970', 1000 * _steps.year // The next update is practically "never".
    ]);
  });
});
//# sourceMappingURL=twitter.test.js.map